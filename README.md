# graphql-redis-resolver-cache
GraphQL resolver cache with Redis with DataLoaders

#### Install
```$ yarn```

#### Run `redis` and `mongo`
```$ docker-compose up -d mongo redis```

#### Run
```$ yarn start```
